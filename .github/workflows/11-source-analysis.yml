# Generated by scripts/code_analysis/generate_source_workflow.py; do not edit by hand.
name: Exact Source Analysis
run-name: Source analysis ${{ inputs.request_id }}
'on':
  workflow_dispatch:
    inputs:
      target:
        required: true
        type: string
      tooling_sha:
        required: true
        type: string
      request_id:
        required: true
        type: string
permissions:
  contents: read
concurrency:
  group: source-${{ fromJSON(inputs.target).id }}
  cancel-in-progress: true
jobs:
  identity:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      jobs: ${{ steps.identity.outputs.jobs }}
      languages: ${{ steps.identity.outputs.languages }}
    steps:
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        persist-credentials: false
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        repository: ${{ fromJSON(inputs.target).source_repository }}
        ref: ${{ fromJSON(inputs.target).head_sha }}
        path: .source
        persist-credentials: false
    - name: Validate trusted tooling and selected target
      id: identity
      env:
        TARGET_JSON: ${{ inputs.target }}
        TOOLING_SHA: ${{ inputs.tooling_sha }}
      run: python -m scripts.code_analysis.source_identity
  scanner-1-sonarqube-cloud:
    name: SonarQube Cloud (Optional)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_API_TOKEN: ${{ secrets.SONAR_API_TOKEN }}
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
    steps:
    - name: Checkout exact scan commit
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        fetch-depth: 0
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Checkout trusted Sonar tooling
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
        repository: ${{ github.repository }}
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Determine whether Sonar analysis is needed
      id: sonar-scope
      env:
        EVENT_NAME: ${{ github.event_name }}
        BEFORE_SHA: ${{ github.event.before }}
        BASE_SHA: ${{ github.event.pull_request.base.sha }}
        SCAN_SHA: ${{ fromJSON(inputs.target).head_sha }}
      shell: bash
      run: "set -euo pipefail\nshould_scan=false\nif [[ \"$EVENT_NAME\" == workflow_dispatch ]]; then\n  should_scan=true\n\
        else\n  base=\"$BEFORE_SHA\"\n  if [[ \"$EVENT_NAME\" == pull_request ]]; then\n    base=\"$BASE_SHA\"\n\
        \  fi\n  if [[ -z \"$base\" || \"$base\" =~ ^0+$ ]] || ! git cat-file -e \"${base}^{commit}\"; then\n  \
        \  should_scan=true\n  elif git diff --name-only \"$base\" \"$SCAN_SHA\" | grep -Eq \\\n    '^(backend/app/|webui/src/|scripts/code_analysis/export_sonar_issues\\\
        .py$|sonar-project\\.properties$|\\.github/workflows/01-code-quality\\.yml$)'; then\n    should_scan=true\n\
        \  fi\nfi\nprintf 'should_scan=%s\\n' \"$should_scan\" >> \"$GITHUB_OUTPUT\"\n"
    - name: Ensure Browse for the verified Sonar API user
      if: env.SONAR_API_TOKEN != ''
      env:
        SCAN_BRANCH: ${{ fromJSON(inputs.target).branch || github.head_ref || github.ref_name }}
      run: 'python $RUNNER_TEMP/analysis-tooling/scripts/code_analysis/ensure_sonar_browse.py

        cat sonar-browse-grant.json

        '
      continue-on-error: true
    - name: Derive stable Sonar analysis branch
      id: sonar-identity
      env:
        SOURCE_BRANCH: ${{ fromJSON(inputs.target).branch || github.head_ref || github.ref_name }}
        DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
      shell: bash
      run: "set -euo pipefail\nsonar_branch=\"$SOURCE_BRANCH\"\nif [[ \"$SOURCE_BRANCH\" != \"$DEFAULT_BRANCH\"\
        \ ]]; then\n  branch_hash=\"$(printf '%s' \"$SOURCE_BRANCH\" | sha256sum | cut -c1-16)\"\n  sonar_branch=\"\
        branch-issue-wall-$branch_hash\"\nfi\nprintf 'sonar_branch=%s\\n' \"$sonar_branch\" >> \"$GITHUB_OUTPUT\"\
        \n"
    - name: Probe Sonar credentials without exposing secrets
      if: env.SONAR_TOKEN != '' || env.SONAR_API_TOKEN != ''
      env:
        SCAN_BRANCH: ${{ steps.sonar-identity.outputs.sonar_branch }}
      run: 'python $RUNNER_TEMP/analysis-tooling/scripts/code_analysis/probe_sonar_access.py

        cat sonar-access-probe.json


        python -c ''import json; p=json.load(open("sonar-access-probe.json")); print("native_export_ready="+str(p["credentials"].get("issue_api",{}).get("project_issues_http_status")==200
        and p["credentials"].get("issue_api",{}).get("branch_issues_http_status") not in (401,403)).lower())'' >>
        "$GITHUB_OUTPUT"

        python -c ''import json; p=json.load(open("sonar-access-probe.json")); print("access_failure="+p["credentials"].get("issue_api",{}).get("access_failure",""))''
        >> "$GITHUB_OUTPUT"'
      id: sonar-access
    - name: Record unconfigured status
      if: steps.sonar-scope.outputs.should_scan == 'true' && env.SONAR_TOKEN == ''
      shell: bash
      run: 'printf ''%s\n'' ''{"schema_version":"1","scanner_family":"SonarQube Cloud","status":"NOT_CONFIGURED","reason":"SONAR_TOKEN
        is unavailable (expected for external fork pull requests)"}'' > sonar-status.json

        '
    - name: Record out-of-scope status
      if: steps.sonar-scope.outputs.should_scan != 'true'
      shell: bash
      run: 'printf ''%s\n'' ''{"schema_version":"1","scanner_family":"SonarQube Cloud","status":"NOT_APPLICABLE","reason":"No
        product source or Sonar configuration changed"}'' > sonar-status.json

        '
    - name: Analyze native push or pull-request event
      id: sonar-native
      if: steps.sonar-scope.outputs.should_scan == 'true' && env.SONAR_TOKEN != '' && github.event_name != 'workflow_dispatch'
        && steps.sonar-access.outputs.native_export_ready == 'true'
      continue-on-error: true
      uses: SonarSource/sonarqube-scan-action@22918119ff8e1ca75a623e15c8296b6ea4fbe28f
      with:
        args: -Dproject.settings=${{ runner.temp }}/analysis-tooling/sonar-project.properties ${{ github.event_name
          == 'push' && format('-Dsonar.branch.name={0} -Dsonar.scm.revision={1}', steps.sonar-identity.outputs.sonar_branch,
          github.sha) || '' }}
      env:
        SONAR_TOKEN: ${{ env.SONAR_TOKEN }}
    - name: Analyze manually selected branch head
      id: sonar-manual
      if: steps.sonar-scope.outputs.should_scan == 'true' && env.SONAR_TOKEN != '' && github.event_name == 'workflow_dispatch'
        && steps.sonar-access.outputs.native_export_ready == 'true'
      continue-on-error: true
      uses: SonarSource/sonarqube-scan-action@22918119ff8e1ca75a623e15c8296b6ea4fbe28f
      with:
        args: -Dproject.settings=${{ runner.temp }}/analysis-tooling/sonar-project.properties -Dsonar.branch.name=${{
          steps.sonar-identity.outputs.sonar_branch }} -Dsonar.scm.revision=${{ fromJSON(inputs.target).head_sha
          }}
      env:
        SONAR_TOKEN: ${{ env.SONAR_TOKEN }}
    - name: Export native Sonar issues for Issue Wall
      id: sonar-export
      if: always() && steps.sonar-scope.outputs.should_scan == 'true' && env.SONAR_API_TOKEN != '' && (steps.sonar-native.outcome
        == 'success' || steps.sonar-manual.outcome == 'success')
      continue-on-error: true
      env:
        SCAN_BRANCH: ${{ fromJSON(inputs.target).branch || github.head_ref || github.ref_name }}
        SCAN_SHA: ${{ fromJSON(inputs.target).head_sha }}
        PULL_REQUEST: ${{ github.event.pull_request.number }}
        SONAR_BRANCH: ${{ steps.sonar-identity.outputs.sonar_branch }}
      shell: bash
      run: "python $RUNNER_TEMP/analysis-tooling/scripts/code_analysis/export_sonar_issues.py \\\n  --report-task\
        \ .scannerwork/report-task.txt \\\n  --output sonar-native-issues.json \\\n  --project combustrrr_Agentic-Kibana\
        \ \\\n  --branch \"$SCAN_BRANCH\" \\\n  --sonar-branch \"$SONAR_BRANCH\" \\\n  --commit \"$SCAN_SHA\" \\\
        \n  --pull-request \"$PULL_REQUEST\"\n"
    - name: Record configured scan status
      if: always() && steps.sonar-scope.outputs.should_scan == 'true' && env.SONAR_TOKEN != ''
      env:
        EVENT_NAME: ${{ github.event_name }}
        NATIVE_OUTCOME: ${{ steps.sonar-native.outcome }}
        MANUAL_OUTCOME: ${{ steps.sonar-manual.outcome }}
        EXPORT_OUTCOME: ${{ steps.sonar-export.outcome }}
        EXPORT_READY: ${{ steps.sonar-access.outputs.native_export_ready }}
        ACCESS_FAILURE: ${{ steps.sonar-access.outputs.access_failure }}
      shell: bash
      run: "outcome=\"$NATIVE_OUTCOME\"\nif [[ \"$EVENT_NAME\" == workflow_dispatch ]]; then\n  outcome=\"$MANUAL_OUTCOME\"\
        \nfi\nstatus=CONFIGURED_COMPLETE\nreason=\"\"\nif [[ \"$outcome\" != success ]]; then\n  status=CONFIGURED_PARTIAL\n\
        \  reason=\"SonarQube Cloud analysis failed; inspect the scanner log\"\nelif [[ -z \"$SONAR_API_TOKEN\"\
        \ ]]; then\n  status=CONFIGURED_PARTIAL\n  reason=\"SONAR_API_TOKEN with project Browse permission is not\
        \ configured\"\nelif [[ \"$EXPORT_OUTCOME\" != success ]]; then\n  status=CONFIGURED_PARTIAL\n  reason=\"\
        SonarQube Cloud analysis completed but native issue export failed\"\nfi\nif [[ \"$EXPORT_READY\" != true\
        \ ]]; then\n  reason=\"Sonar native issue API is unavailable to the configured credential; analysis awaits\
        \ export access\"\nfi\nif [[ \"$ACCESS_FAILURE\" == branch_entitlement ]]; then\n  reason=\"Sonar organization\
        \ denies non-main-branch data access; enable branch entitlement. Token validity and Browse permission do\
        \ not resolve this restriction.\"\nfi\njq -n --arg status \"$status\" --arg reason \"$reason\" --arg outcome\
        \ \"$outcome\" --arg export \"$EXPORT_OUTCOME\" \\\n  '{schema_version:\"1\",scanner_family:\"SonarQube\
        \ Cloud\",status:$status,reason:$reason,surfaces:{analysis:$outcome,native_issue_export:$export}}' \\\n\
        \  > sonar-status.json\n"
    - name: Upload SonarQube Cloud status
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: sonar-results
        path: 'sonar-status.json

          sonar-access-probe.json

          sonar-browse-grant.json

          sonar-native-issues.json

          .scannerwork/report-task.txt

          '
        if-no-files-found: error
        retention-days: 7
    needs:
    - identity
    permissions:
      contents: read
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'sonarqube-cloud') }}
  scanner-1-python-ruff:
    name: Ruff (Python Lint + Format)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        fetch-depth: 0
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Checkout trusted analysis tooling
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
        repository: ${{ github.repository }}
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Set up Python 3.11
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.11.16
        cache: pip
        cache-dependency-path: ${{ runner.temp }}/analysis-tooling/.ci/requirements.txt
    - name: Install pinned tools
      run: python -m pip install --requirement $RUNNER_TEMP/analysis-tooling/.ci/requirements.txt
    - name: Enforce code-analysis workflow policy
      run: python $RUNNER_TEMP/analysis-tooling/scripts/code_analysis/audit_workflows.py
    - name: "Ruff lint (default rules \u2014 matches existing python-static CI gate)"
      run: python -m ruff check backend/app backend/tests --exit-zero
    - name: "Ruff lint (extended rules \u2014 advisory for baseline phase)"
      run: "python -m ruff check backend/ \\\n  --config $RUNNER_TEMP/analysis-tooling/config/code-analysis/ruff.toml\
        \ \\\n  --output-format=github \\\n  --exit-zero\n"
    - name: Ruff format check (advisory)
      run: "python -m ruff format --check backend/ \\\n  --config $RUNNER_TEMP/analysis-tooling/config/code-analysis/ruff.toml\
        \ \\\n  || true\n"
    - name: Ruff lint (JSON for normalizer)
      if: always()
      run: "python -m ruff check backend/ \\\n  --config $RUNNER_TEMP/analysis-tooling/config/code-analysis/ruff.toml\
        \ \\\n  --output-format=json \\\n  --exit-zero \\\n  --output-file ruff-results.json\n"
    - name: Upload Ruff results
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: ruff-results
        path: ruff-results.json
        if-no-files-found: error
        retention-days: 7
    needs:
    - identity
    permissions:
      contents: read
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'python-ruff') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-1-python-bandit:
    name: Bandit (Python Security)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Checkout trusted analysis tooling
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
        repository: ${{ github.repository }}
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Set up Python 3.11
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.11.16
        cache: pip
        cache-dependency-path: ${{ runner.temp }}/analysis-tooling/.ci/requirements.txt
    - name: Install tools
      run: python -m pip install --requirement $RUNNER_TEMP/analysis-tooling/.ci/requirements.txt
    - name: Run Bandit (JSON, advisory for baseline)
      run: "bandit \\\n  -r backend/ \\\n  -c backend/pyproject.toml \\\n  --format json \\\n  --output bandit-results.json\
        \ \\\n  --exit-zero\n"
    - name: Normalize Bandit JSON to SARIF
      run: "mkdir -p bandit-scan-results\ncp bandit-results.json bandit-scan-results/\npython $RUNNER_TEMP/analysis-tooling/scripts/code_analysis/normalizer.py\
        \ \\\n  --input-dir bandit-scan-results \\\n  --output-dir bandit-normalized\n"
    - name: Report HIGH severity findings (informational)
      if: always()
      run: "python - <<'PY'\nimport json\ndata = json.load(open(\"bandit-results.json\", encoding=\"utf-8\"))\n\
        results = data.get(\"results\", [])\ncounts = {level: sum(r.get(\"issue_severity\") == level for r in results)\n\
        \          for level in (\"CRITICAL\", \"HIGH\", \"MEDIUM\", \"LOW\")}\nprint(f\"Bandit findings: {len(results)}\
        \ total ({counts})\")\nfor finding in results:\n    if finding.get(\"issue_severity\") in {\"CRITICAL\"\
        , \"HIGH\"}:\n        print(f\"  {finding.get('test_id', '')} | {finding.get('filename', '')}:\"\n     \
        \         f\"{finding.get('line_number', '')} | {finding.get('issue_text', '')[:100]}\")\nPY\n"
    - name: Build bounded GitHub Security SARIF view
      run: "python $RUNNER_TEMP/analysis-tooling/scripts/code_analysis/bound_sarif.py \\\n  --input bandit-normalized/normalized.sarif\
        \ \\\n  --output bandit-normalized/github-security.sarif \\\n  --maximum 5000\n"
    - name: Upload Bandit results artifact
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: bandit-results
        path: 'bandit-results.json

          bandit-normalized/normalized.sarif

          '
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'python-bandit') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-1-python-types:
    name: Pyright (Python Type Checking)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Set up Python 3.11
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.11.16
        cache: pip
        cache-dependency-path: backend/requirements-dev.txt
    - name: Install dependencies + Pyright
      run: 'python -m pip install --upgrade pip

        pip install -r backend/requirements.txt

        pip install -r backend/requirements-dev.txt

        pip install "pyright==1.1.411"

        '
    - name: Run Pyright (advisory)
      run: "pyright backend/app \\\n  --outputjson > pyright-results.json \\\n  || true\n"
    - name: Upload Pyright results
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: pyright-results
        path: pyright-results.json
        retention-days: 7
    needs:
    - identity
    permissions:
      contents: read
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'python-types') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-1-typescript-quality:
    name: ESLint + tsc (TypeScript/React)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: webui
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        repository: ${{ github.repository }}
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Set up Node.js 22
      uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444
      with:
        node-version: 22.23.2
        cache: npm
        cache-dependency-path: webui/package-lock.json
    - name: Install locked dependencies
      run: python -I "$RUNNER_TEMP/analysis-tooling/scripts/code_analysis/trusted_entry.py" run_profile javascript-install
        --source "$GITHUB_WORKSPACE"
    - name: TypeScript type check
      shell: bash
      run: 'set +e

        npx tsc --noEmit --pretty false 2>&1 | tee tsc-results.txt

        exit 0

        '
    - name: ESLint lint (human-readable advisory output)
      run: npx eslint "src/**/*.{ts,tsx}" --format stylish || true
    - name: "ESLint lint (fail on error \u2014 matches existing webui-lint CI gate)"
      continue-on-error: true
      run: npx eslint "src/**/*.{ts,tsx}"
    - name: Upload ESLint results (for normalizer)
      if: always()
      run: 'npx eslint "src/**/*.{ts,tsx}" --format json --output-file eslint-results.json || true

        '
    - name: Upload ESLint results artifact
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: eslint-results
        path: webui/eslint-results.json
        retention-days: 7
    - name: Upload TypeScript results artifact
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: tsc-results
        path: webui/tsc-results.txt
        retention-days: 7
    - name: Run repository JavaScript test profile
      if: always()
      run: python -I "$RUNNER_TEMP/analysis-tooling/scripts/code_analysis/trusted_entry.py" run_profile javascript-test
        --source "$GITHUB_WORKSPACE"
    - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      if: always()
      with:
        name: typescript-tests
        path: typescript-test-status.json
        retention-days: 7
    needs:
    - identity
    permissions:
      contents: read
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'typescript-quality') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-2-codeql:
    name: CodeQL (Semantic SAST)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        language: ${{ fromJSON(needs.identity.outputs.languages) }}
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        fetch-depth: 2
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Checkout trusted analysis tooling
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
        repository: ${{ github.repository }}
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Stage trusted CodeQL policy in Git metadata
      run: 'mkdir -p "$GITHUB_WORKSPACE/.git/code-analysis"

        cp "$RUNNER_TEMP/analysis-tooling/.github/codeql/codeql-config.yml" "$GITHUB_WORKSPACE/.git/code-analysis/codeql-config.yml"'
    - name: Initialize CodeQL
      uses: github/codeql-action/init@b96794f015dfd88f77b49b1c93e0fa7110f94c63
      with:
        languages: ${{ matrix.language }}
        queries: +security-extended,security-and-quality
        config-file: .git/code-analysis/codeql-config.yml
    - name: "Build (Python \u2014 no explicit build needed)"
      if: matrix.language == 'python'
      run: "echo \"Python \u2014 autobuild skipped, using source\""
    - name: Autobuild (TypeScript/React)
      if: matrix.language == 'javascript-typescript'
      uses: github/codeql-action/autobuild@b96794f015dfd88f77b49b1c93e0fa7110f94c63
    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@b96794f015dfd88f77b49b1c93e0fa7110f94c63
      with:
        category: /language:${{ matrix.language }}
        output: codeql-output
        upload: false
    - name: Canonicalize CodeQL SARIF artifact
      shell: bash
      env:
        CODEQL_LANGUAGE: ${{ matrix.language }}
      run: "set -euo pipefail\nmapfile -d '' sarif_files < <(find codeql-output -type f -name '*.sarif' -print0)\n\
        if (( ${#sarif_files[@]} != 1 )); then\n  echo \"::error::Expected exactly one CodeQL SARIF file, found\
        \ ${#sarif_files[@]}\"\n  exit 1\nfi\ncp \"${sarif_files[0]}\" \"codeql-${CODEQL_LANGUAGE}.sarif\"\n"
    - name: Upload SARIF artifact (for normalizer)
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: codeql-${{ matrix.language }}-sarif
        path: codeql-${{ matrix.language }}.sarif
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'codeql') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-2-semgrep:
    name: Semgrep (Pattern Analysis)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        fetch-depth: 0
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Checkout trusted analysis tooling
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
        repository: ${{ github.repository }}
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Set up Python 3.11
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.11.16
    - name: Install Semgrep
      run: pip install semgrep==1.80.0 click==8.1.7 python-dateutil==2.9.0.post0
    - name: Validate custom Semgrep rules
      run: semgrep --validate --config=$RUNNER_TEMP/analysis-tooling/.github/semgrep-rules/
    - name: Run Semgrep (JSON output)
      run: "semgrep \\\n  --config=p/python \\\n  --config=p/owasp-top-ten \\\n  --config=p/jwt \\\n  --config=p/secrets\
        \ \\\n  --config=p/fastapi \\\n  --config=$RUNNER_TEMP/analysis-tooling/.github/semgrep-rules/ \\\n  --json\
        \ \\\n  --output=semgrep-results.json \\\n  --severity=ERROR \\\n  --severity=WARNING \\\n  backend/ webui/src/\
        \ || true\n"
    - name: Normalize Semgrep JSON to SARIF
      run: "mkdir -p semgrep-scan-results\ncp semgrep-results.json semgrep-scan-results/\npython $RUNNER_TEMP/analysis-tooling/scripts/code_analysis/normalizer.py\
        \ \\\n  --input-dir semgrep-scan-results \\\n  --output-dir semgrep-normalized\n"
    - name: Upload SARIF artifact (for normalizer)
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: semgrep-sarif
        path: semgrep-normalized/normalized.sarif
        retention-days: 7
    - name: Upload JSON results (for normalizer)
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: semgrep-results
        path: semgrep-results.json
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'semgrep') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-3-snyk:
    name: Snyk (Optional SCA + Code)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
      SNYK_CONFIGURED: ${{ secrets.SNYK_TOKEN != '' }}
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        repository: ${{ github.repository }}
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Record unconfigured status
      if: env.SNYK_CONFIGURED != 'true'
      shell: bash
      run: 'printf ''%s\n'' ''{"schema_version":"1","scanner_family":"Snyk","status":"NOT_CONFIGURED","reason":"SNYK_TOKEN
        is not configured"}'' > snyk-status.json

        '
    - name: Install pinned Snyk CLI
      if: env.SNYK_CONFIGURED == 'true'
      run: npm install --global snyk@1.1306.4
    - name: Set up Python 3.11 for complete pip dependency trees
      if: env.SNYK_CONFIGURED == 'true'
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.11.16
        cache: pip
        cache-dependency-path: '.ci/requirements.txt

          backend/requirements.txt

          docs/requirements.txt

          '
    - name: Resolve Python dependency manifests for Snyk SCA
      if: env.SNYK_CONFIGURED == 'true'
      shell: bash
      run: python -I $RUNNER_TEMP/analysis-tooling/scripts/code_analysis/snyk_metadata.py --source "$GITHUB_WORKSPACE"
        --destination "$RUNNER_TEMP/snyk-metadata"
    - name: Scan open-source dependencies
      id: snyk-sca
      if: env.SNYK_CONFIGURED == 'true'
      continue-on-error: true
      shell: bash
      run: 'set -o pipefail

        python -I $RUNNER_TEMP/analysis-tooling/scripts/code_analysis/snyk_scan.py 2>&1 | tee snyk-open-source.log'
      env:
        SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    - name: Scan source code
      id: snyk-code
      if: env.SNYK_CONFIGURED == 'true'
      continue-on-error: true
      shell: bash
      run: 'set +e

        snyk code test --sarif-file-output=snyk-code.sarif 2>&1 | tee snyk-code.log

        scan_exit=${PIPESTATUS[0]}

        set -e

        # Exit 1 means findings. Exit 2+ means unavailable/invalid analysis.

        [[ "$scan_exit" -le 1 ]] || exit "$scan_exit"

        '
      env:
        SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    - name: Record configured scan status
      if: always() && env.SNYK_CONFIGURED == 'true'
      env:
        SCA_OUTCOME: ${{ steps.snyk-sca.outcome }}
        CODE_OUTCOME: ${{ steps.snyk-code.outcome }}
      shell: bash
      run: "status=CONFIGURED_COMPLETE\nreason=\"\"\nif [[ \"$SCA_OUTCOME\" != success || \"$CODE_OUTCOME\" != success\
        \ ]]; then\n  status=CONFIGURED_PARTIAL\n  reason=\"One or more Snyk analysis surfaces were unavailable\
        \ or incomplete; inspect the retained logs\"\nfi\nif [[ \"$SCA_OUTCOME\" != success || \"$CODE_OUTCOME\"\
        \ != success ]] && grep -q \"monthly limit\" snyk-*.log; then\n  reason=\"Snyk execution incomplete; a quota\
        \ warning was also emitted. Inspect the failing manifest or surface before attributing the failure to quota.\"\
        \nfi\njq -n \\\n  --arg status \"$status\" \\\n  --arg reason \"$reason\" \\\n  --arg sca \"$SCA_OUTCOME\"\
        \ \\\n  --arg code \"$CODE_OUTCOME\" \\\n  '{schema_version:\"1\",scanner_family:\"Snyk\",status:$status,reason:$reason,surfaces:{sca:$sca,code:$code}}'\
        \ \\\n  > snyk-status.json\n"
    - name: Upload structured Snyk evidence
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: snyk-results
        path: 'snyk-status.json

          snyk-open-source.sarif

          snyk-code.sarif

          snyk-open-source.log

          snyk-code.log

          '
        if-no-files-found: error
        retention-days: 7
    - name: Upload isolated resolver diagnostics
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: snyk-resolver-diagnostics
        path: ${{ runner.temp }}/snyk-resolver-error.json
        if-no-files-found: ignore
        retention-days: 7
    needs:
    - identity
    permissions:
      contents: read
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'snyk') }}
  scanner-3-osv-scanner:
    name: OSV-Scanner (Dependency CVEs)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        fetch-depth: 0
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Run OSV-Scanner
      continue-on-error: true
      uses: google/osv-scanner-action/osv-scanner-action@6e4298ebc4db23e847df9b2e2de2939d6f066c67
      with:
        scan-args: '--lockfile=backend/requirements.txt

          --lockfile=backend/requirements-dev.txt

          --lockfile=webui/package-lock.json

          --format=sarif

          --output=osv-results.sarif'
    - name: Upload artifact (for normalizer)
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: osv-results
        path: osv-results.sarif
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'osv-scanner') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-3-gitleaks:
    name: Gitleaks (Secrets Detection)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
    - name: Checkout (full history for historical scan)
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        fetch-depth: 0
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Run pinned Gitleaks without PR mutation
      run: docker run --rm -v "$PWD:/repo" zricethezav/gitleaks:v8.24.2 detect --source=/repo --report-format=sarif
        --report-path=/repo/gitleaks-results.sarif --redact --exit-code=0
    - name: Validate redacted Gitleaks output
      run: jq -e '(.runs | type) == "array"' gitleaks-results.sarif > /dev/null
    - name: Upload Gitleaks results
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: gitleaks-results
        path: gitleaks-results.sarif
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'gitleaks') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-3-trivy:
    name: Trivy (Filesystem & Container Security)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Run Trivy (filesystem scan)
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
      with:
        scan-type: fs
        scan-ref: .
        format: sarif
        output: trivy-fs.sarif
        severity: CRITICAL,HIGH
        exit-code: '0'
    - name: Run Trivy (config scan)
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
      with:
        scan-type: config
        scan-ref: .
        format: sarif
        output: trivy-config.sarif
        severity: CRITICAL,HIGH
    - name: Upload artifacts
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: trivy-results
        path: 'trivy-fs.sarif

          trivy-config.sarif

          '
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'trivy') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-3-hadolint:
    name: Hadolint (Dockerfile Lint)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Run Hadolint on all Dockerfiles
      uses: hadolint/hadolint-action@54c9adbab1582c2ef04b2016b760714a4bfde3cf
      with:
        dockerfile: backend/Dockerfile
        format: sarif
        output-file: hadolint.sarif
        no-fail: true
    - name: Upload Hadolint artifact
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: hadolint-results
        path: hadolint.sarif
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'hadolint') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-3-checkov:
    name: Checkov (IaC Security)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Set up Python 3.11
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.11.16
    - name: Install Checkov
      run: pip install "checkov==3.2.468"
    - name: Run Checkov
      run: "checkov \\\n  --directory . \\\n  --framework dockerfile,github_actions \\\n  --output sarif \\\n  --output-file-path\
        \ checkov.sarif \\\n  --soft-fail || true\n"
    - name: Canonicalize Checkov SARIF artifact
      shell: bash
      run: "set -euo pipefail\nif [[ -d checkov.sarif ]]; then\n  mapfile -d '' sarif_files < <(find checkov.sarif\
        \ -type f -name '*.sarif' -print0)\n  if (( ${#sarif_files[@]} != 1 )); then\n    echo \"::error::Expected\
        \ exactly one Checkov SARIF file, found ${#sarif_files[@]}\"\n    exit 1\n  fi\n  cp \"${sarif_files[0]}\"\
        \ checkov-results.sarif\nelif [[ -f checkov.sarif ]]; then\n  cp checkov.sarif checkov-results.sarif\nelse\n\
        \  echo \"::error::Checkov did not produce SARIF evidence\"\n  exit 1\nfi\n"
    - name: Upload artifact
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: checkov-results
        path: checkov-results.sarif
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'checkov') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-3-shipping-image-security:
    name: Shipping Images (CVEs + SBOM + License Policy)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
    steps:
    - name: Checkout selected source
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Checkout trusted SBOM policy tooling
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
        repository: ${{ github.repository }}
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Build exact-commit shipping images
      env:
        SOURCE_SHA: ${{ fromJSON(inputs.target).head_sha }}
      shell: bash
      run: "set -euo pipefail\nbuild_date=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"\ncommon=(--build-arg \"TLSOC_BUILD_SHA=$SOURCE_SHA\"\
        \ --build-arg \"TLSOC_BUILD_DATE=$build_date\")\ndocker build \"${common[@]}\" -t agentic-soc-backend:analysis\
        \ backend\ndocker build \"${common[@]}\" -t agentic-soc-webui:analysis -f webui/Dockerfile .\ndocker image\
        \ inspect agentic-soc-backend:analysis agentic-soc-webui:analysis \\\n  > shipping-image-inspect.json\n"
    - name: Scan backend image vulnerabilities
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
      with:
        scan-type: image
        image-ref: agentic-soc-backend:analysis
        format: sarif
        output: trivy-backend-image.sarif
        severity: CRITICAL,HIGH,MEDIUM,LOW
        exit-code: '0'
    - name: Scan web UI image vulnerabilities
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
      with:
        scan-type: image
        image-ref: agentic-soc-webui:analysis
        format: sarif
        output: trivy-webui-image.sarif
        severity: CRITICAL,HIGH,MEDIUM,LOW
        exit-code: '0'
    - name: Generate backend CycloneDX inventory
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
      with:
        scan-type: image
        image-ref: agentic-soc-backend:analysis
        format: cyclonedx
        output: backend.cdx.json
    - name: Generate web UI CycloneDX inventory
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
      with:
        scan-type: image
        image-ref: agentic-soc-webui:analysis
        format: cyclonedx
        output: webui.cdx.json
    - name: Generate backend SPDX inventory
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
      with:
        scan-type: image
        image-ref: agentic-soc-backend:analysis
        format: spdx-json
        output: backend.spdx.json
    - name: Generate web UI SPDX inventory
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
      with:
        scan-type: image
        image-ref: agentic-soc-webui:analysis
        format: spdx-json
        output: webui.spdx.json
    - name: Evaluate SBOM license policy and emit provenance
      env:
        SOURCE_SHA: ${{ fromJSON(inputs.target).head_sha }}
      shell: bash
      run: "set -euo pipefail\npython $RUNNER_TEMP/analysis-tooling/scripts/code_analysis/validate_sbom.py \\\n\
        \  backend.cdx.json webui.cdx.json backend.spdx.json webui.spdx.json \\\n  --status sbom-policy-status.json\
        \ --sarif sbom-license-policy.sarif\njq -n \\\n  --arg repository \"$GITHUB_REPOSITORY\" --arg commit \"\
        ${SOURCE_SHA,,}\" \\\n  --arg run_id \"$GITHUB_RUN_ID\" \\\n  --slurpfile images shipping-image-inspect.json\
        \ \\\n  '{_type:\"https://in-toto.io/Statement/v1\",subject:($images[0]|map({name:(.RepoTags[0]//\"local-image\"\
        ),digest:{sha256:(.Id|sub(\"^sha256:\";\"\"))}})),predicateType:\"https://slsa.dev/provenance/v1\",predicate:{buildDefinition:{buildType:\"\
        agentic-soc/github-actions-shipping-images/v1\",externalParameters:{repository:$repository,commit:$commit}},runDetails:{builder:{id:\"\
        github-actions\"},metadata:{invocationId:$run_id}}}}' \\\n  > shipping-image-provenance.intoto.json\nimage_findings=\"\
        $(jq -s '[.[].runs[]?.results[]?] | length' \\\n  trivy-backend-image.sarif trivy-webui-image.sarif)\"\n\
        jq -n --argjson findings \"$image_findings\" \\\n  '{schema_version:\"1\",scanner_family:\"Shipping Image\
        \ Trivy\",status:\"COMPLETED_OPTIONAL\",reason:\"Both exact-commit shipping images were built and scanned\"\
        ,finding_count:$findings,observation_count:$findings}' \\\n  > shipping-image-trivy-status.json\n"
    - name: Retain image security evidence
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: shipping-image-security
        path: 'trivy-backend-image.sarif

          trivy-webui-image.sarif

          sbom-license-policy.sarif

          sbom-policy-status.json

          backend.cdx.json

          webui.cdx.json

          backend.spdx.json

          webui.spdx.json

          shipping-image-inspect.json

          shipping-image-provenance.intoto.json

          shipping-image-trivy-status.json

          '
        if-no-files-found: error
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'shipping-image-security') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-3-workflow-security-posture:
    name: Workflow Security (zizmor + Scorecard + Secret Posture)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
    - name: Checkout selected source
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Install and run pinned zizmor
      shell: bash
      run: 'set -euo pipefail

        pip install "zizmor==1.29.0"

        set +e

        zizmor --format sarif .github > zizmor-results.sarif

        scan_exit=$?

        set -e

        [[ "$scan_exit" -le 1 ]] || exit "$scan_exit"

        '
    - name: Observe GitHub secret-scanning posture
      env:
        GH_TOKEN: ${{ secrets.SECURITY_POSTURE_TOKEN || github.token }}
      shell: bash
      run: "set -euo pipefail\nrepo_json=\"$(gh api \"repos/${SOURCE_REPOSITORY}\")\"\nsecret_scanning=\"$(jq -r\
        \ '.security_and_analysis.secret_scanning.status // \"UNAVAILABLE\"' <<<\"$repo_json\")\"\npush_protection=\"\
        $(jq -r '.security_and_analysis.secret_scanning_push_protection.status // \"UNAVAILABLE\"' <<<\"$repo_json\"\
        )\"\nalerts_status=UNAVAILABLE\nalert_count=null\nif gh api --paginate \"repos/${SOURCE_REPOSITORY}/secret-scanning/alerts?state=open&per_page=100\"\
        \ \\\n    --jq '.[].number' > secret-scanning-alert-numbers.txt 2>secret-scanning-api.log; then\n  alerts_status=OBSERVED\n\
        \  alert_count=\"$(wc -l < secret-scanning-alert-numbers.txt)\"\nelse\n  : > secret-scanning-alert-numbers.txt\n\
        fi\nposture_status=CONFIGURED_COMPLETE\nposture_reason=\"Secret scanning and push protection enabled; open-alert\
        \ count observed without secret values\"\nif [[ \"$secret_scanning\" == UNAVAILABLE || \"$push_protection\"\
        \ == UNAVAILABLE || \"$alerts_status\" == UNAVAILABLE ]]; then\n  posture_status=CONFIGURED_PARTIAL\n  posture_reason=\"\
        GitHub token cannot observe one or more secret-protection controls; configure a read-only SECURITY_POSTURE_TOKEN\"\
        \nelif [[ \"$secret_scanning\" != enabled || \"$push_protection\" != enabled ]]; then\n  posture_status=CONFIGURED_PARTIAL\n\
        \  posture_reason=\"One or more GitHub secret-protection controls require repository-owner activation\"\n\
        fi\njq -n --arg status \"$posture_status\" --arg reason \"$posture_reason\" \\\n  --arg scanning \"$secret_scanning\"\
        \ \\\n  --arg protection \"$push_protection\" --arg alerts \"$alerts_status\" \\\n  --argjson count \"$alert_count\"\
        \ \\\n  '{schema_version:\"1\",scanner_family:\"Repository Security Posture\",status:$status,reason:$reason,secret_scanning:$scanning,push_protection:$protection,alerts_status:$alerts,open_alert_count:$count,finding_count:0,observation_count:0}'\
        \ \\\n  > repository-security-posture-status.json\n"
    - name: Retain workflow and platform posture
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: workflow-security-posture
        path: 'zizmor-results.sarif

          repository-security-posture-status.json

          secret-scanning-alert-numbers.txt

          secret-scanning-api.log

          '
        if-no-files-found: error
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'workflow-security-posture') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-3-openssf-scorecard:
    name: OpenSSF Scorecard (Repository Supply Chain)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ inputs.tooling_sha }}
        persist-credentials: false
    - name: Install verified Scorecard 5.5.0
      run: 'curl --fail --location --retry 3 https://github.com/ossf/scorecard/releases/download/v5.5.0/scorecard_5.5.0_linux_amd64.tar.gz
        -o scorecard.tar.gz

        echo "83b90a05c1540ef1390db1cd5711e5fd04be9c1d8537fb84d39d02092d6a8dff  scorecard.tar.gz" | sha256sum --check

        tar -xzf scorecard.tar.gz scorecard

        '
    - name: Scan external repository at selected commit
      id: scorecard
      continue-on-error: true
      env:
        GITHUB_AUTH_TOKEN: ${{ github.token }}
        ENABLE_SARIF: 'true'
        SOURCE_SHA: ${{ fromJSON(inputs.target).head_sha }}
      run: './scorecard --repo="github.com/$SOURCE_REPOSITORY" --commit="$SOURCE_SHA" --format=json --show-details
        > scorecard-native.json

        python -I scripts/code_analysis/scorecard_report.py'
    - name: Retain Scorecard execution status
      if: always()
      env:
        OUTCOME: ${{ steps.scorecard.outcome }}
      run: "if [[ \"$OUTCOME\" != success ]]; then\n  jq -n '{scanner_family:\"OpenSSF Scorecard\",status:\"OPERATIONAL_FAILURE\"\
        ,reason:\"Native Scorecard execution or source identity validation failed\"}' > scorecard-status.json\n\
        fi"
    - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      if: always()
      with:
        name: openssf-scorecard
        path: 'openssf-scorecard.sarif

          scorecard-status.json

          scorecard-native.json'
        retention-days: 7
    needs:
    - identity
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'openssf-scorecard') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-4-complexity:
    name: Radon + Xenon (Code Complexity)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Checkout trusted analysis tooling
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
        repository: ${{ github.repository }}
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Set up Python 3.11
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.11.16
        cache: pip
        cache-dependency-path: ${{ runner.temp }}/analysis-tooling/.ci/requirements.txt
    - name: Install tools
      run: python -m pip install --requirement $RUNNER_TEMP/analysis-tooling/.ci/requirements.txt
    - name: "Radon \u2014 cyclomatic complexity report"
      run: 'echo "=== Cyclomatic Complexity (all functions) ==="

        radon cc backend/ -s -a --min A --json > radon-cc.json

        radon cc backend/ -s -a --min B

        echo "=== Maintainability Index ==="

        radon mi backend/ -s --json > radon-mi.json

        radon mi backend/ -s

        '
    - name: "Xenon \u2014 enforce complexity gate"
      run: "# Fail if any module exceeds:\n# --max-average B  (allow avg up to B)\n# --max-modules B  (allow any\
        \ module up to B)\n# --max-absolute C (hard stop at C \u2014 very complex)\nxenon \\\n  --max-average B\
        \ \\\n  --max-modules B \\\n  --max-absolute C \\\n  backend/ 2>&1 | tee xenon-results.txt || true\n"
    - name: Upload Radon reports
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: radon-reports
        path: 'radon-cc.json

          radon-mi.json

          xenon-results.txt

          '
        retention-days: 7
    needs:
    - identity
    permissions:
      contents: read
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'complexity') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-4-dead-code:
    name: Vulture (Dead Code Detection)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - name: Checkout trusted analysis tooling
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
        repository: ${{ github.repository }}
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Set up Python 3.11
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.11.16
        cache: pip
        cache-dependency-path: ${{ runner.temp }}/analysis-tooling/.ci/requirements.txt
    - name: Install tools
      run: python -m pip install --requirement $RUNNER_TEMP/analysis-tooling/.ci/requirements.txt
    - name: Run Vulture
      run: "vulture backend/ \\\n  --min-confidence 80 \\\n  > vulture-results.txt || true\n# Print human-readable\
        \ summary\nvulture backend/ --min-confidence 80 || true\n"
    - name: Upload Vulture results
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: vulture-results
        path: vulture-results.txt
        retention-days: 7
    needs:
    - identity
    permissions:
      contents: read
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'dead-code') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-4-test-coverage:
    name: Coverage.py (Test Coverage)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - name: Checkout
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ fromJSON(inputs.target).head_sha }}
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        repository: ${{ github.repository }}
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Set up Python 3.11
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.11.16
        cache: pip
        cache-dependency-path: backend/requirements-dev.txt
    - name: Install dependencies
      run: python -I "$RUNNER_TEMP/analysis-tooling/scripts/code_analysis/trusted_entry.py" run_profile python-install
        --source "$GITHUB_WORKSPACE"
    - name: Run pytest with coverage
      env:
        PYTHONPATH: ${{ github.workspace }}/backend
      run: python -I "$RUNNER_TEMP/analysis-tooling/scripts/code_analysis/trusted_entry.py" run_profile python-test
        --source "$GITHUB_WORKSPACE"
    - name: Materialize coverage reports after failed or successful tests
      if: always()
      working-directory: backend
      run: 'python -m coverage json -o coverage.json || true

        python -m coverage xml -o coverage.xml || true

        '
    - name: Upload coverage reports
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: coverage-reports
        path: 'backend/coverage.xml

          backend/coverage.json

          backend/coverage-status.json

          '
        retention-days: 7
    needs:
    - identity
    permissions:
      contents: read
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'test-coverage') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-7-schemathesis-fuzz:
    name: Schemathesis API Fuzzer
    runs-on: ubuntu-latest
    timeout-minutes: 35
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: kavach_test
        ports:
        - 5432:5432
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
    steps:
    - name: Checkout Code
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
        ref: ${{ fromJSON(inputs.target).head_sha }}
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        repository: ${{ github.repository }}
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Set up Python 3.11
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.11.16
        cache: pip
        cache-dependency-path: backend/requirements-dev.txt
    - name: Install dependencies & Schemathesis
      run: 'python -I "$RUNNER_TEMP/analysis-tooling/scripts/code_analysis/trusted_entry.py" run_profile python-install
        --source "$GITHUB_WORKSPACE"

        python -m pip install schemathesis==3.39.16 httpx==0.27.2'
    - name: Run API Fuzzing (Schemathesis)
      id: schemathesis
      run: python -I "$RUNNER_TEMP/analysis-tooling/scripts/code_analysis/trusted_entry.py" run_profile api-fuzz
        --source "$GITHUB_WORKSPACE"
    - name: Upload Fuzzing Report
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: schemathesis-report
        path: 'fuzzing-results.xml

          schema.json

          schemathesis-status.json'
        if-no-files-found: error
        retention-days: 7
    needs:
    - identity
    permissions:
      contents: read
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'schemathesis-fuzz') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
  scanner-7-atheris-state-machine:
    name: Atheris (Bounded Decision-State Fuzzing)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
    - name: Checkout exact workflow target
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        persist-credentials: false
        repository: ${{ fromJSON(inputs.target).source_repository }}
        ref: ${{ fromJSON(inputs.target).head_sha }}
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        repository: ${{ github.repository }}
        ref: ${{ inputs.tooling_sha }}
        path: .analysis-tooling
        persist-credentials: false
    - name: Keep trusted tooling outside the source scan tree
      run: mv "$GITHUB_WORKSPACE/.analysis-tooling" "$RUNNER_TEMP/analysis-tooling"
    - name: Set up Python 3.12
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: 3.12.14
        cache: pip
        cache-dependency-path: backend/requirements.txt
    - name: Install bounded fuzz target
      shell: bash
      run: 'set -euo pipefail

        pip install -r backend/requirements.txt "atheris==3.0.0"

        '
    - name: Run deterministic case-decision fuzz campaign
      id: atheris
      shell: bash
      run: "set -euo pipefail\nmkdir -p fuzz-corpus fuzz-crashes\nprintf '\\x00\\xff\\x32\\x01' > fuzz-corpus/seed-false-positive\n\
        printf '\\x01\\xff\\x64\\x02' > fuzz-corpus/seed-true-positive\ncd backend\npython $RUNNER_TEMP/analysis-tooling/tests/security_canary/fuzzing/atheris_state_fuzzer.py\
        \ \\\n  -runs=25000 -max_len=4096 -timeout=5 \\\n  -artifact_prefix=../fuzz-crashes/ ../fuzz-corpus\ncd\
        \ ..\njq -n '{schema_version:\"1\",scanner_family:\"Atheris\",status:\"COMPLETED_OPTIONAL\",reason:\"25,000\
        \ bounded inputs executed against deterministic case decision logic\",finding_count:0,observation_count:0}'\
        \ \\\n  > atheris-status.json\n"
    - name: Record Atheris operational failure
      if: failure() && steps.atheris.outcome == 'failure'
      shell: bash
      run: "jq -n '{schema_version:\"1\",scanner_family:\"Atheris\",status:\"OPERATIONAL_FAILURE\",reason:\"The\
        \ bounded Atheris campaign did not complete; inspect the job log and retained crash input.\",finding_count:0,observation_count:0}'\
        \ \\\n  > atheris-status.json\n"
    - name: Retain Atheris campaign evidence
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: atheris-state-fuzzing
        path: 'atheris-status.json

          fuzz-crashes/

          '
        if-no-files-found: warn
        retention-days: 7
    needs:
    - identity
    permissions:
      contents: read
    if: ${{ contains(fromJSON(needs.identity.outputs.jobs), 'atheris-state-machine') }}
    env:
      SOURCE_REPOSITORY: ${{ fromJSON(inputs.target).source_repository }}
      ANALYSIS_BACKEND_ROOT: ${{ github.workspace }}/backend
      ANALYSIS_SOURCE_SHA: ${{ fromJSON(inputs.target).head_sha }}
  report:
    needs:
    - identity
    - scanner-1-sonarqube-cloud
    - scanner-1-python-ruff
    - scanner-1-python-bandit
    - scanner-1-python-types
    - scanner-1-typescript-quality
    - scanner-2-codeql
    - scanner-2-semgrep
    - scanner-3-snyk
    - scanner-3-osv-scanner
    - scanner-3-gitleaks
    - scanner-3-trivy
    - scanner-3-hadolint
    - scanner-3-checkov
    - scanner-3-shipping-image-security
    - scanner-3-workflow-security-posture
    - scanner-3-openssf-scorecard
    - scanner-4-complexity
    - scanner-4-dead-code
    - scanner-4-test-coverage
    - scanner-7-schemathesis-fuzz
    - scanner-7-atheris-state-machine
    if: ${{ always() && needs.identity.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      actions: read
    steps:
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        ref: ${{ inputs.tooling_sha }}
        persist-credentials: false
    - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
      with:
        python-version: '3.11'
    - name: Install trusted reporting dependencies
      run: python -m pip install click==8.2.1 ruff==0.12.5
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        repository: ${{ fromJSON(inputs.target).source_repository }}
        ref: ${{ fromJSON(inputs.target).head_sha }}
        path: .source
        persist-credentials: false
    - name: Collect exact producer artifacts and assemble report
      env:
        GH_TOKEN: ${{ github.token }}
        TARGET_JSON: ${{ inputs.target }}
        TOOLING_SHA: ${{ inputs.tooling_sha }}
      run: python -m scripts.code_analysis.source_identity --assemble
    - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: hosted-report-${{ github.run_id }}-${{ github.run_attempt }}
        path: .hosted/report/
        if-no-files-found: error
        retention-days: 7
