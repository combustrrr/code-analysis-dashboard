name: Code analysis source
run-name: Source analysis ${{ inputs.request_id }}
on:
  workflow_dispatch:
    inputs:
      target:
        type: string
        required: true
      tooling_sha:
        type: string
        required: true
      request_id:
        type: string
        required: true
permissions:
  contents: read
jobs:
  analyze:
    if: ${{ fromJSON(inputs.target).profile_mode == 'portable' }}
    permissions:
      contents: read
      actions: read
    uses: combustrrr/code-analysis-dashboard/.github/workflows/reusable-source.yml@baaa7e24070774e634faaf419cfcb77e4ee92f6b
    with:
      tooling_sha: baaa7e24070774e634faaf419cfcb77e4ee92f6b
      target: ${{ inputs.target }}
      request_id: ${{ inputs.request_id }}
  configured-profile:
    if: ${{ fromJSON(inputs.target).profile_mode == 'agentic-soc' }}
    permissions:
      contents: read
      actions: read
    uses: combustrrr/code-analysis-dashboard/.github/workflows/reusable-source-full.yml@baaa7e24070774e634faaf419cfcb77e4ee92f6b
    with:
      tooling_sha: baaa7e24070774e634faaf419cfcb77e4ee92f6b
      target: ${{ inputs.target }}
      request_id: ${{ inputs.request_id }}
    secrets:
      SECURITY_POSTURE_TOKEN: ${{ secrets.SECURITY_POSTURE_TOKEN }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      SONAR_API_TOKEN: ${{ secrets.SONAR_API_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
