---
title: Code-analysis handoff 2026-09-02
description: Current implementation, branch, validation, evidence, and next-action handoff.
---

# Code-analysis handoff — 2026-09-02

## Current outcome

The code-analysis subsystem provides exact-commit, read-only scanner collection and a
self-contained offline Issue Wall artifact. **Full Code Analysis (Manual)** is the sole
supported publication entry point. The artifact displays branch/SHA identity, actionable
severity ordering, file links, evidence drill-down, channel status, and integrity details.

## Branches

- Working branch: `feature/static-code-analysis`
- Upstream integration target, if later approved: `Testing` (not `main`)

The local `upstream` remote is fetch-only; its push URL is disabled. No upstream pull
request, branch, merge, or repository-setting change was made.

## Integrated evidence

Required evidence covers 16 channels across CodeQL, Semgrep, Bandit, Ruff, Pyright,
ESLint, TypeScript, OSV-Scanner, Gitleaks, Trivy, Hadolint, Checkov, Vulture, Radon,
Xenon, and Coverage.py. Optional lanes include SonarQube Cloud, Snyk, CodeRabbit
`AI_ADVISORY`, shipping-image/SBOM evidence, workflow posture, and Scorecard.

Only real current evidence is rendered. Missing optional evidence is reported as
unavailable and never converted into a clean result.

## Validation state

- The upstream-scoped integration candidate passed the workflow audit, CI contract, and
  111 combined policy/service tests.
- A later exact-SHA manual run successfully built the offline Issue Wall and artifact
  handoff. Fixed historical run IDs and counts were removed from public documentation;
  use the latest successful run for the commit under review.
- SonarQube Cloud and workflow-based Snyk scanning completed successfully in the fork.
  CodeRabbit did not return fresh evidence during the latest external-review attempt, so
  the dashboard represented that lane as unavailable.

## Next safe action

Wait for review. If integration is approved, create one new clean branch from the latest
upstream `Testing`, apply only the paths listed in `UPSTREAM_INTEGRATION.md`, rerun all
validation and the manual Issue Wall, and request approval before opening an upstream PR.

Do not merge the long-lived feature branch or the temporary review base into upstream.
